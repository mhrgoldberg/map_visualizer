import React from "react";

export default function Dashboard({file}) {
  const parseData = (file) => {
    debugger;

  }

  return (
    <React.Fragment>
      {parseData(file)}
    </React.Fragment>
  );
}
