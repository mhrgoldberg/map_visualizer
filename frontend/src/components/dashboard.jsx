import React from "react";

export default function Dashboard({file}) {
  const parseData = () => {
    // debugger

  }

  return (
    <React.Fragment>
      {parseData()}
    </React.Fragment>
  );
}
